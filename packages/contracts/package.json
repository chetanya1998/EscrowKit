{
    "name": "contracts",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "forge build",
        "test": "forge test",
        "deploy": "forge script script/Deploy.s.sol:DeployScript --rpc-url http://localhost:8545 --broadcast",
        "lint": "forge fmt --check",
        "fmt": "forge fmt"
    },
    "dependencies": {
        "@openzeppelin/contracts": "^5.4.0"
    }
}